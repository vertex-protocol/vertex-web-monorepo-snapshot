import { WithChildren } from '@vertex-protocol/web-common';

export function MoneyMarketsSectionTitle({ children }: WithChildren) {
  return <h2 className="text-text-primary text-xl">{children}</h2>;
}
